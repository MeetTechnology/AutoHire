# 06 Implementation Plan

## 当前进度（2026-04-11）

- 当前代码库已完成文档中 `Slice 1` 到 `Slice 6` 的 MVP 实现
- 当前已打通专家端主流程：`/apply` -> `/apply/resume` -> `/apply/result` -> `/apply/materials`
- 当前已完成 token 校验、会话恢复、申请快照、状态机驱动跳转、分析三态、结构化补填、材料上传、提交幂等
- 当前已提供本地可运行的 `mock` 分析模式、内存数据模式、Prisma migration 与 seed
- 当前已完成真实 PostgreSQL 建库、migration、seed 与关键状态落库验证
- 当前已完成申请唯一性、分析关联与材料软删除审计字段的 schema hardening
- 当前已完成阿里云 OSS 预签名 `PUT` 上传的服务端联调验证，对象 key 规则已稳定
- 当前浏览器端 OSS 直传在本地 E2E 中已基本可用，但回归稳定性仍需继续收口
- 当前仍待完成的外部联调部分包括：受控下载 / 预览链路、既有简历分析服务 `live` 模式、真实邀约数据校验

## 当前切片完成情况

### Slice 1: token 识别与申请初始化

状态：已完成

已实现内容：

- 支持通过 `token` 进入系统
- 支持通过 HttpOnly 会话恢复申请上下文
- 已实现 `expert-session` 申请快照接口
- token 无效时可返回异常入口态

### Slice 2: 简历上传与分析中状态

状态：已完成

已实现内容：

- 支持简历 upload intent + confirm 两步式上传
- 上传后可创建分析任务并进入 `CV_ANALYZING`
- 分析中状态可轮询

### Slice 3: 分析结果三态

状态：已完成

已实现内容：

- 已接入统一的简历分析适配层
- 支持 `INELIGIBLE`、`INFO_REQUIRED`、`ELIGIBLE` 三态结果
- 页面按后端快照驱动展示

### Slice 4: 结构化补填与二次分析

状态：已完成

已实现内容：

- 可根据 `missing_fields` 动态渲染结构化字段
- 提交补充字段后可触发二次分析
- 二次分析结果可继续驱动状态流转

### Slice 5: 材料上传

状态：已完成

已实现内容：

- 已实现 6 类材料上传与分类回显
- 支持删除与重复进入恢复
- 支持分批保存与材料摘要返回

### Slice 6: 最终提交与已提交恢复

状态：已完成

已实现内容：

- 已实现提交接口幂等
- 已提交状态可恢复
- 页面可显示“已收到材料信息，将在 1-3 个工作日内答复”

## 联调里程碑（2026-04-11）

### Milestone A: 主流程 MVP 完成

状态：已完成

产出：

- 专家端四个主页面与状态机驱动流转已打通
- mock 分析模式下的首次分析、补填、材料上传、提交链路已完成
- 端到端主路径已具备自动化回归基础

### Milestone B: 真实数据库接入

状态：已完成

产出：

- 本地已创建 `autohire` PostgreSQL 数据库
- 已执行 Prisma `migrate deploy`
- 已执行 `db:seed`
- 已验证初始化、恢复、补填、删除材料、提交等关键状态真实落库
- 已补齐关键唯一约束、外键关联、简历版本绑定与材料删除时间戳

### Milestone C: OSS 预签名上传联调

状态：已完成（服务端） / 进行中（浏览器端稳定性）

产出：

- 已切换 `FILE_STORAGE_MODE=oss`
- 已完成 `resume/materials` 两类 upload intent 的真实预签名 URL 生成
- 已验证服务端真实 `PUT` 上传成功
- 已验证材料回显与删除链路

剩余：

- 补齐受控下载 / 预览接口
- 继续收口浏览器端直传 E2E 稳定性

### Milestone D: 真实简历分析服务联调

状态：未开始

目标：

- 切换 `RESUME_ANALYSIS_MODE=live`
- 冻结 `status/result/missing_fields` 返回结构
- 验证失败、超时、重试场景

## 1. 实施策略

采用适合 Vibe Coding 的纵向切片方式实施，每一轮都尽量做到前后端联通、状态可验证、文档可回写。

## 2. 切片规划

### Slice 1: token 识别与申请初始化

目标：

- 支持通过 `token` 进入系统
- 创建或恢复申请记录
- 完成介绍页与申请快照接口

完成标准：

- 访问链接即可识别专家
- token 无效时返回异常页
- 可返回当前申请状态

### Slice 2: 简历上传与分析中状态

目标：

- 支持简历上传
- 创建分析任务
- 分析中页可轮询状态

完成标准：

- 上传成功后状态转入 `CV_ANALYZING`
- 分析状态接口可正常工作

### Slice 3: 分析结果三态

目标：

- 接入既有简历分析结果
- 完成三态结果页

完成标准：

- 能正确进入 `INELIGIBLE`
- 能正确进入 `INFO_REQUIRED`
- 能正确进入 `ELIGIBLE`

### Slice 4: 结构化补填与二次分析

目标：

- 渲染 `missing_fields`
- 提交补充字段
- 触发二次分析

完成标准：

- 可动态渲染字段
- 二次分析后返回新结果

### Slice 5: 材料上传

目标：

- 实现 6 类材料上传与回显
- 支持分批保存

完成标准：

- 每类可批量上传
- 已传材料可回显与删除

### Slice 6: 最终提交与已提交恢复

目标：

- 实现最终提交
- 显示提交成功提示
- 再次访问时显示已提交状态

完成标准：

- 提交接口幂等
- 状态转为 `SUBMITTED`

## 3. 每个切片的标准执行步骤

1. 先重读相关 PRD 段落
2. 确认状态流转影响
3. 只读取与当前切片相关的文件
4. 先改接口和数据，再改页面
5. 先跑最小验证，再继续下一个切片
6. 回写文档变化

## 4. 联调顺序建议

1. `expert-session`
2. `resume upload`
3. `analysis-status`
4. `analysis-result`
5. `supplemental-fields`
6. `materials`
7. `submit`

## 5. 交付节奏建议

- 第 1 周：Slice 1-2
- 第 2 周：Slice 3-4
- 第 3 周：Slice 5
- 第 4 周：Slice 6 + 联调 + 提测

## 6. 当前后续重点

- 第一优先级：补齐阿里云 OSS 的受控下载 / 预览接口
- 第二优先级：稳定浏览器端直传 E2E 与测试前置数据
- 第三优先级：联调既有简历分析服务并冻结返回结构
- 第四优先级：补齐真实环境下的集成验证与回归测试
